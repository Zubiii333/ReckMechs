# Railway Environment Variables
PORT=8080

# Database Configuration (Railway will auto-generate if using their DB)
# DATABASE_URL=sqlite:backend/config/workshop.db

# Application Settings
APP_ENV=production
APP_DEBUG=false